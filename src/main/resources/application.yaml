spring:
  data:
    mongodb:
      database: Teachers
      host: localhost
      port: 27018
  freemarker:
    suffix: .ftl


management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always